# Cloud Run runtime
PORT=8080
NODE_ENV=production

# Supabase
SUPABASE_URL=https://wltqkxesvtfwotcngzjj.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Gemini (LLM + embedding)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
GEMINI_EMBEDDING_MODEL=text-embedding-004

# CORS — comma-separated origins (Firebase Hosting + custom domain)
CORS_ORIGIN=https://emco-8a3b5.web.app,https://emco-8a3b5.firebaseapp.com,http://localhost:5000,http://localhost:3000

# IP hashing salt (for ip_hash in emco_chat_sessions)
IP_HASH_SALT=change-me-to-a-random-32+-char-secret
